import { InvoiceEntity, ChargeEntity } from '@/domain/entities';

export const CHARGES_TABLES: Map<string, ChargeEntity> = new Map();
export const INVOICES_TABLE: Map<string, InvoiceEntity> = new Map();
